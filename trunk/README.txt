Christian Grise and Matthew Nebel

To run our example scenes after compiling using the Makefile:

./simulation -both fluid_drop_only.txt cloth_drop.txt -timestep 0.01 -collision both
./simulation -both fluid_pour.txt cloth_pool.txt -timestep 0.01 -collision both